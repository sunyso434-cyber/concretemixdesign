# 增量更新包配置说明

## 问题描述
用户反映没有增量安装包，只有完整安装包。

## 解决方案

### 1. 配置增量更新
在 `package.json` 中添加了以下配置：

```json
{
  "build": {
    "nsis": {
      "differentialPackage": true
    }
  }
}
```

### 2. 增量更新工作原理
- **完整包**：`混凝土配合比设计软件-1.0.2-x64.exe` (约90MB)
- **增量包**：当有新版本时，只包含变更的文件，大大减少下载大小

### 3. 部署增量更新
1. 构建新版本时，electron-builder 会自动生成增量包
2. 将所有文件上传到更新服务器
3. electron-updater 会自动检测并下载合适的包（完整包或增量包）

### 4. 文件说明
- `混凝土配合比设计软件-1.0.2-x64.exe` - 完整安装包
- `latest.yml` - 版本信息和文件校验
- 其他文件用于自动更新验证

### 5. 使用方法
用户安装后，应用会自动检查更新：
- 如果是小版本更新，下载增量包
- 如果是大版本更新或首次安装，下载完整包

这样可以显著减少更新时的下载时间和带宽消耗。