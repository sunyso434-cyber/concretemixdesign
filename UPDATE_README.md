# 混凝土配合比设计软件 v1.0.2

## 更新说明

### 版本 1.0.2 更新内容
- 新增原材料单价管理功能
- 修复方案管理页面白屏问题
- 修复配合比成本计算参数
- **新增自动更新功能**

### 自动更新设置

本应用支持自动更新。要启用自动更新功能，请按照以下步骤操作：

#### 1. 设置更新服务器
您需要一个支持静态文件托管的服务器（如GitHub Pages、AWS S3、阿里云OSS等）来存放更新文件。

#### 2. 上传更新文件
将 `dist-1.0.2` 目录下的所有文件上传到您的更新服务器。

#### 3. 创建 latest.yml 文件
在更新服务器上创建 `latest.yml` 文件，内容如下：

```yaml
version: 1.0.2
files:
  - url: 混凝土配合比设计软件-1.0.2-x64.exe
    sha512: [计算文件的SHA512哈希值]
    size: 90276300
path: 混凝土配合比设计软件-1.0.2-x64.exe
sha512: [计算文件的SHA512哈希值]
releaseDate: '2026-03-31T10:46:00.000Z'
```

#### 4. 修改配置文件
更新 `electron-builder.yml` 中的URL：

```yaml
provider: generic
url: https://your-actual-server.com/updates  # 替换为您的实际服务器地址
channel: latest
```

#### 5. 重新构建应用
修改配置后，重新运行构建命令生成新版本。

### 手动更新
如果自动更新不可用，用户可以：
1. 下载最新版本的安装包
2. 运行安装包进行更新
3. 应用会自动替换旧版本

### 技术支持
如有问题，请联系技术支持团队。